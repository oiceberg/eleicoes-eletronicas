# .env.example - Copie para .env e preencha os valores
# .env - ARQUIVO CONFIDENCIAL (NÃO COMPARTILHE)

# =========================================================
# ===== SEGREDOS E PARÂMETROS DE GERAÇÃO (keys_generator) =====
# =========================================================

# Frase longa e complexa usada como semente criptográfica.
# É o segredo mestre que nunca deve ser compartilhado.
MASTER_SECRET=troque_isso_por_uma_frase_secreta_longa_e_unica

# Salt (Sal) da eleição. Altere a cada pleito.
ELECTION_SALT=ELEICOES_AGESP_2025

# Tamanho da Chave Privada (e.g., 12 letras).
PRIVATE_LEN=12

# Arquivo de entrada para o GERADOR (Lista base de eleitores aptos)
# Duas colunas: Nome Completo, Endereço de e-mail
INPUT_CSV=eleitores_base.csv

# ========================================================
# ===== ARQUIVOS PONTUAIS (Entrada e Log do mailer) =====
# ========================================================

# Nome do arquivo CONFIDENCIAL que o MAILER LÊ.
# Deve corresponder ao nome que o keys_generator.py gera (Padrão: envio_confidencial.csv)
# Este é o único arquivo que liga Nome, Email, Chave Pública e Chave Privada.
CSV_INPUT=envio_confidencial.csv

# Log do MAILER (Onde é registrado quem já recebeu e-mail com sucesso)
CSV_LOG=envios_log.csv

# =================================================
# ===== CONFIGURAÇÃO DE E-MAIL (mailer) =====
# =================================================

# Servidor SMTP e Porta
SMTP_HOST=smtp.provedor.com
SMTP_PORT=465
USE_SSL=true

# Credenciais de Login
SMTP_USER=remetente@provedor.com.br
SMTP_PASS=troque_isso_pela_senha_do_email

# Informações de Identidade
ANO_ELEICAO=2025
FROM_NAME=Comissão Eleitoral AGESP
FROM_EMAIL=remetente@provedor.com.br
ASSUNTO=Eleições AGESP {ano} – Suas chaves para votação

# Flags de Controle
# Se TRUE, não envia e-mails (apenas exibe no terminal).
SIMULACAO=TRUE

# Pacing e retentativa
EMAILS_POR_LOTE=30
PAUSA_ENTRE_LOTES_SEC=180
DELAY_MIN_SEC=6
DELAY_MAX_SEC=12
MAX_TENTATIVAS=3

# =================================================
# ===== GOOGLE FORMS (Pré-preenchimento) =====
# =================================================

# URL Base do Forms (copie a URL quando estiver editando o Forms)
BASE_FORM_URL=https://docs.google.com/forms/d/e/XPTO/viewform?usp=pp_url

# IDs de Entrada dos Campos no Forms
# Para obter esses IDs, use a função 'Obter Link Pré-Preenchido' no Google Forms.
ENTRY_PUBLIC_KEY=entry.33333333
ENTRY_PRIVATE_KEY=entry.5555555555
